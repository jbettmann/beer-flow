import BreweriesPageLS from "@/components/LoadingSkeleton/BreweriesPageLS";
import React from "react";

type Props = {};

const loading = (props: Props) => {
  return <BreweriesPageLS />;
};

export default loading;
