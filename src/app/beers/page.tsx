import axios from "axios";

import { FC } from "react";

// export const revalidate = 10;

// equivilate to getServerSideProps
const Pages: FC = ({}) => {
  // const { data } = await axios.get("https://beer-bible-api.vercel.app/users");

  return <div>Beers</div>;
};

export default Pages;
